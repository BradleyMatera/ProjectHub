const projects = [{ name: "Gatsby Blog", desc: "A blogging app" }, { name: "Pokedex", desc: "Pokémon data app" }]; const chatDiv = document.createElement("div"); chatDiv.id = "bradley-chat"; chatDiv.innerHTML = "<input id=\"chat-input\" placeholder=\"Ask me anything!\"><div id=\"chat-output\"></div>"; document.body.appendChild(chatDiv); const input = document.getElementById("chat-input"); input.addEventListener("keypress", async (e) => { if (e.key === "Enter") { const msg = input.value; const res = await fetch("https://your-vercel-url/api/chat", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ message: msg }) }); const data = await res.json(); document.getElementById("chat-output").innerHTML += `<p>${data.reply}</p>`; input.value = ""; } }); console.log("ProjectHub loaded!");
